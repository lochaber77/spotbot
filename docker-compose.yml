services:
  # Unofficial WhatsApp HTTP API. Keeps the messy session handling in its own
  # container and exposes a webhook (inbound) + REST (send). The published image
  # is amd64-only, so on the arm64 Mac mini it runs under emulation via the
  # platform pin below (Docker Desktop / Rosetta). On amd64 hosts it's native.
  waha:
    image: devlikeapro/waha:latest
    platform: linux/amd64
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - WHATSAPP_HOOK_URL=${WEBHOOK_URL}
      - WHATSAPP_HOOK_EVENTS=message
      - WAHA_API_KEY=${WAHA_API_KEY}
      - TZ=${TZ}
    volumes:
      - waha-sessions:/app/.sessions

  # The "brain": FastAPI webhook receiver + Claude. SQLite lives on a volume so
  # data survives restarts (and is a single file you can copy on cutover).
  app:
    build: ./app
    restart: unless-stopped
    depends_on:
      - waha
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL}
      - WAHA_URL=${WAHA_URL}
      - WAHA_SESSION=${WAHA_SESSION}
      - WAHA_API_KEY=${WAHA_API_KEY}
      - ALLOWED_NUMBERS=${ALLOWED_NUMBERS}
      - TZ=${TZ}
      # Shared calendar (M3). Blank GOOGLE_CALENDAR_ID keeps it disabled.
      - GOOGLE_CALENDAR_ID=${GOOGLE_CALENDAR_ID}
      - GOOGLE_SERVICE_ACCOUNT_JSON=${GOOGLE_SERVICE_ACCOUNT_JSON:-/secrets/service-account.json}
      - CONFIRMATION_TTL_MINUTES=${CONFIRMATION_TTL_MINUTES:-30}
      # Gmail drafts (M4). Enabled per-member when a token file exists here.
      - GMAIL_TOKENS_DIR=${GMAIL_TOKENS_DIR:-/secrets/gmail}
    volumes:
      - app-data:/data
      # To enable the shared calendar, drop the service-account JSON at
      # GOOGLE_SERVICE_ACCOUNT_HOST_PATH and uncomment this read-only mount:
      # - ${GOOGLE_SERVICE_ACCOUNT_HOST_PATH:-./secrets/service-account.json}:/secrets/service-account.json:ro
      # To enable Gmail drafts, put per-member tokens under GMAIL_TOKENS_HOST_PATH
      # (see scripts/gmail_authorize.py) and uncomment this read-only mount:
      # - ${GMAIL_TOKENS_HOST_PATH:-./secrets/gmail}:/secrets/gmail:ro

volumes:
  waha-sessions:
  app-data:
